<?xml version="1.0"?>
<robot xmlns:xacro="http://www.ros.org/wiki/xacro" name="goat">
    
    <xacro:arg name="gpu" default="false"/>
    <xacro:property name="gpu" value="$(arg gpu)" />
    <!--No GPU, based on original goat urdf-->

    <!-- the base footprint, no dimensions but used by other stuff-->
    <link name="base_footprint"/>

    <!--apparently a mass between the encoders-->
    <link name="base_link">
        <inertial>
        </inertial>
    </link>
</robot>